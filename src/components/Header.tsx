import React, { FC } from 'react'

type Props = {}

export const Header: FC<Props> = (props) => {
    return (
        <header className='flex justify-center items-center h-20 w-full bg-emerald-400'>

        </header>
    )
}